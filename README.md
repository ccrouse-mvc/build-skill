# build-skill

The `/build` Claude Code skill and the agents it spawns (`coder`, `coder-ui`, `verifier`).

Live copies are in `~/.claude/skills/build/` and `~/.claude/agents/`; that is what Claude Code
loads. This repo is their history: run `sync.ps1`, review the diff, commit.
